% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/radarPlotCm.R
\name{radarPlotCm}
\alias{radarPlotCm}
\title{Create Radar Plots for Confusion Matrix Metrics}
\usage{
radarPlotCm(
  cmList,
  methods = NULL,
  metrics = c("Accuracy", "Sensitivity", "Specificity", "Precision", "Recall"),
  plotTitle = "Radar Plot of Metrics",
  colors = NULL,
  layout = "single",
  displayArea = FALSE,
  displayFill = TRUE,
  saveData = FALSE,
  fileName = NULL,
  ...
)
}
\arguments{
\item{cmList}{A named list of \code{confusionMatrix} objects. Names of the list
elements correspond to the method names.}

\item{methods}{An optional character vector of method names to plot. If \code{NULL}
(default) and the session is interactive, the user is prompted to select
methods. In a non-interactive session, \code{NULL} results in all methods
being plotted.}

\item{metrics}{A character vector of metrics to visualize. Defaults to
c("Accuracy", "Sensitivity", "Specificity", "Precision", "Recall").}

\item{plotTitle}{An optional title for the radar plot.}

\item{colors}{An optional character vector of colors for each method. If
\code{NULL}, a default palette is used.}

\item{layout}{Specifies the layout: "single" for a combined plot or
"multiple" for one plot per method. Defaults to "single".}

\item{displayArea}{A logical value; if \code{TRUE}, displays the polygon area.}

\item{displayFill}{A logical value; if \code{TRUE}, fills radar polygons with a
semi-transparent color.}

\item{saveData}{A logical value; if \code{TRUE}, saves the radar data to a CSV.}

\item{fileName}{A character string specifying the file name if \code{saveData} is
\code{TRUE}.}

\item{...}{Additional arguments passed to \code{fmsb::radarchart}.}
}
\value{
The function is called for its side effect of creating a plot and
returns \code{invisible(NULL)}. If required packages are missing, it also
returns \code{invisible(NULL)} after printing a message.
}
\description{
Generates radar plots (spider plots) to visualize and compare performance
metrics from confusion matrices. Requires the 'fmsb' and 'scales' packages.
}
\examples{
if (requireNamespace("caret", quietly = TRUE) &&
    requireNamespace("fmsb", quietly = TRUE) &&
    requireNamespace("scales", quietly = TRUE)) {

  # --- 1. Create a sample list of confusionMatrix objects ---
  set.seed(123)
  ref <- factor(sample(c("A", "B"), 50, replace = TRUE))
  pred1 <- factor(sample(c("A", "B"), 50, replace = TRUE))
  pred2 <- factor(sample(c("A", "B"), 50, replace = TRUE))
  sampleCmList <- list(Method1 = caret::confusionMatrix(pred1, ref),
                       Method2 = caret::confusionMatrix(pred2, ref))

  # --- 2. Run non-interactive examples ---
  # Redirect graphics to a null device to prevent plots opening during checks
  png(tempfile())

  radarPlotCm(
    cmList = sampleCmList,
    methods = "Method1",
    plotTitle = "Performance of Method1"
  )

  dev.off() # Close the null device

} else {
  message("Skipping radar plot examples because 'fmsb' or 'scales' is not installed.")
}
}
