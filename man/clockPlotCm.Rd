% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clockPlotCm.R
\name{clockPlotCm}
\alias{clockPlotCm}
\title{Create Clock Plots for Confusion Matrix Metrics}
\usage{
clockPlotCm(
  cmList,
  methods = NULL,
  metrics = c("Accuracy", "Sensitivity", "Specificity", "Precision", "Recall"),
  plotTitle = "Clock Plot of Metrics",
  colors = NULL,
  layout = "single"
)
}
\arguments{
\item{cmList}{A named list of \code{confusionMatrix} objects. Names of the list
elements correspond to the method names.}

\item{methods}{An optional character vector of method names to include in the
plot. If \code{NULL} (default) and the session is interactive, the user will be
prompted to select methods. In a non-interactive session, \code{NULL} results
in all methods being plotted.}

\item{metrics}{A character vector of metric names to display. Defaults to
c("Accuracy", "Sensitivity", "Specificity", "Precision", "Recall").}

\item{plotTitle}{An optional title for the clock plot.}

\item{colors}{An optional character vector of colors for each method. If
\code{NULL}, a default color palette is used.}

\item{layout}{Specifies the layout: "single" for a combined plot or
"multiple" for one plot per method. Defaults to "single".}
}
\value{
A \code{ggplot} or \code{patchwork} object, or \code{invisible(NULL)} if required
packages are not installed.
}
\description{
Generates circular bar plots to visualize performance metrics derived from
confusion matrices. Requires the 'ggplot2', 'patchwork', and 'stringr'
packages.
}
\examples{
if (requireNamespace("caret", quietly = TRUE) &&
    requireNamespace("ggplot2", quietly = TRUE) &&
    requireNamespace("patchwork", quietly = TRUE) &&
    requireNamespace("stringr", quietly = TRUE)) {

  # --- 1. Create a sample list of confusionMatrix objects ---
  set.seed(42)
  ref <- factor(sample(c("A", "B"), 100, replace = TRUE))
  pred1 <- factor(sample(c("A", "B"), 100, replace = TRUE))
  pred2 <- factor(sample(c("A", "B"), 100, replace = TRUE))
  sampleCmList <- list("MethodA" = caret::confusionMatrix(pred1, ref),
                       "MethodB" = caret::confusionMatrix(pred2, ref))

  # --- 2. Run non-interactive examples ---
  p1 <- clockPlotCm(
    cmList = sampleCmList,
    methods = "MethodA",
    plotTitle = "Performance of MethodA"
  )
  # To display the plot in an interactive session, simply type `p1`

} else {
  message("Skipping clock plot examples because dependencies are not installed.")
}
}
