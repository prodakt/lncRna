% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/refGTF.R
\name{getRefBiotypes}
\alias{getRefBiotypes}
\title{Get Gene or Transcript Biotypes from a GRanges Object}
\usage{
getRefBiotypes(gtfObject, level = "transcript")
}
\arguments{
\item{gtfObject}{A \code{GRanges} object. The object's metadata columns must
contain \code{gene_id} and \code{gene_biotype} for gene-level extraction, or
\code{transcript_id} and \code{transcript_biotype} for transcript-level extraction.}

\item{level}{A character string specifying whether to extract information at
the "gene" or "transcript" level. Defaults to "transcript".}
}
\value{
A \code{data.frame} with two columns containing unique identifier-biotype
pairs (e.g., \code{transcript_id} and \code{transcript_biotype}), with rows
containing \code{NA} identifiers removed.
}
\description{
This function extracts unique biotypes for either genes or transcripts from a
\code{GRanges} object, which is the standard representation for genomic features
in Bioconductor (e.g., after importing a GTF file with \code{rtracklayer::import}).
}
\examples{
# --- 1. Create a sample GRanges object for a reproducible example ---
# This object mimics the structure of a GTF file imported with rtracklayer.
sampleGtf <- GenomicRanges::GRanges(
  seqnames = "chr1",
  ranges = IRanges::IRanges(start = c(100, 200, 300), width = 50),
  strand = "+",
  gene_id = c("gene1", "gene1", NA),
  gene_biotype = c("protein_coding", "protein_coding", "lincRNA"),
  transcript_id = c("tx1", "tx2", "tx3"),
  transcript_biotype = c("protein_coding", "retained_intron", "lincRNA")
)

# --- 2. Run the function ---

# Example 2a: Extract transcript biotypes (default level)
transcriptBiotypes <- getRefBiotypes(gtfObject = sampleGtf)
print("Transcript Biotypes:")
print(transcriptBiotypes)

# Example 2b: Extract gene biotypes
geneBiotypes <- getRefBiotypes(gtfObject = sampleGtf, level = "gene")
print("Gene Biotypes:")
print(geneBiotypes)

}
\keyword{GRanges}
\keyword{GTF}
\keyword{biotype}
\keyword{rtracklayer}
