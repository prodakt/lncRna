% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/strGTF2stat.R
\name{getGtfStats}
\alias{getGtfStats}
\title{Extract Transcript Statistics from a GRanges Object}
\usage{
getGtfStats(gtfObject)
}
\arguments{
\item{gtfObject}{A \code{GRanges} object, for example, from \code{rtracklayer::import}.
The object's metadata columns must contain \code{type}, \code{transcript_id}, and
\code{exon_number} for the features of type "exon".}
}
\value{
A \code{data.frame} with columns: \code{transcript_id}, \code{transLength} (total
length of all exons for the transcript), and \code{exons} (the highest exon
number, representing the count of exons).
}
\description{
This function calculates per-transcript statistics, such as the number of exons
and total transcript length (sum of exon lengths), from a \code{GRanges} object
typically created by importing a GTF/GFF file.
}
\examples{
# --- 1. Create a sample GRanges object for a reproducible example ---
sampleGtf <- GenomicRanges::GRanges(
  seqnames = "chr1",
  ranges = IRanges::IRanges(start = c(100, 200, 400, 500), width = c(50, 60, 100, 20)),
  strand = "+",
  type = c("exon", "exon", "transcript", "exon"),
  transcript_id = c("tx1", "tx1", "tx2", "tx2"),
  exon_number = c("1", "2", NA, "1")
)

# --- 2. Run the function ---
transcriptStats <- getGtfStats(gtfObject = sampleGtf)

print("Transcript Statistics:")
print(transcriptStats)

}
\keyword{GRanges}
\keyword{GTF}
\keyword{lncRNA}
\keyword{stringtie}
