% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/testTrainNc.R
\name{splitNcTestTrain}
\alias{splitNcTestTrain}
\title{Split Non-Coding Sequences into Test and Train Sets}
\usage{
splitNcTestTrain(ncIds, trainProportion = 0.6)
}
\arguments{
\item{ncIds}{A character vector of non-coding sequence identifiers, or an
object (like a list from \code{seqinr::read.fasta}) from which names can be
extracted.}

\item{trainProportion}{The proportion (0 to 1) of sequences to allocate to the
training set (default: 0.6).}
}
\value{
A list containing two character vectors:
\item{train}{Sequence identifiers allocated to the training set.}
\item{test}{Sequence identifiers allocated to the testing set.}
}
\description{
Splits a set of non-coding sequence identifiers into training and testing
subsets based on a specified proportion. For reproducible splits, set the
random seed using \code{set.seed()} before calling this function.
}
\examples{
# --- 1. Define a character vector of sequence IDs ---
ncIdsVec <- paste0("nc_seq_", 1:100)

# --- 2. Run the function with a 70/30 split ---
set.seed(123) # for a reproducible example
ncSplit <- splitNcTestTrain(ncIds = ncIdsVec, trainProportion = 0.7)

# --- 3. Verify the output ---
print(paste("Training set size:", length(ncSplit$train)))
print(paste("Test set size:", length(ncSplit$test)))
head(ncSplit$train)

}
\keyword{data}
\keyword{learning}
\keyword{lncRNA}
\keyword{machine}
\keyword{partitioning}
\keyword{sampling}
\keyword{split}
