% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/getBiotypes.R
\name{getBiotypes}
\alias{getBiotypes}
\title{Extract Gene or Transcript Biotypes from a GRanges Object}
\usage{
getBiotypes(refGtf, level = "transcript")
}
\arguments{
\item{refGtf}{A \code{GRanges} object imported from a reference annotation file.
It must contain metadata columns for biotypes (e.g., \code{gene_biotype},
\code{transcript_biotype}) and corresponding IDs (\code{gene_id}, \code{transcript_id}).}

\item{level}{A character string specifying whether to extract information for
\code{"gene"} or \code{"transcript"} features (default: \code{"transcript"}).}
}
\value{
A \code{DataFrame} (from the \code{S4Vectors} package) with two columns:
the identifier (\code{gene_id} or \code{transcript_id}) and the corresponding
biotype (\code{gene_biotype} or \code{transcript_biotype}).
}
\description{
This function extracts unique biotypes for genes or transcripts from a
\code{GRanges} object, typically imported from a reference GTF/GFF file.
}
\examples{
# --- 1. Create a sample GRanges object mimicking a reference GTF ---
sampleRefGtf <- GenomicRanges::GRanges(
  seqnames = "chr1",
  ranges = IRanges::IRanges(start = 1:6, width = 100),
  gene_id = c("G1", "G1", "G2", "G2", "G3", "G3"),
  gene_biotype = c("protein_coding", "protein_coding", "lncRNA",
                   "lncRNA", "pseudogene", "pseudogene"),
  transcript_id = c("T1.1", "T1.2", "T2.1", "T2.1", "T3.1", "T3.1"),
  transcript_biotype = c("protein_coding", "protein_coding_variant",
                         "lncRNA", "lncRNA", "pseudogene", "pseudogene")
)

# --- 2. Extract biotypes at the transcript level ---
transcript_biotypes <- getBiotypes(refGtf = sampleRefGtf, level = "transcript")
print(transcript_biotypes)

# --- 3. Extract biotypes at the gene level ---
gene_biotypes <- getBiotypes(refGtf = sampleRefGtf, level = "gene")
print(gene_biotypes)

}
