% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/CodPotV2.R
\name{codPotToTbl}
\alias{codPotToTbl}
\title{Combine Coding Potential Tool Results}
\usage{
codPotToTbl(
  cpc2Outfile = NULL,
  plekOutfile = NULL,
  feelncOutfile = NULL,
  cpatOutfile = NULL,
  cpatCutoff = 0.364,
  cnciOutfile = NULL,
  lncFinderOutfile = NULL,
  lncRnaMdeepOutfile = NULL
)
}
\arguments{
\item{cpc2Outfile}{Path to the CPC2 output file.}

\item{plekOutfile}{Path to the PLEK output file.}

\item{feelncOutfile}{Path to the FEELnc output file.}

\item{cpatOutfile}{Path to the CPAT output file.}

\item{cpatCutoff}{Cutoff value for CPAT probability (default: 0.364).}

\item{cnciOutfile}{Path to the CNCI output file.}

\item{lncFinderOutfile}{Path to the LncFinder output file.}

\item{lncRnaMdeepOutfile}{Path to the lncRNA_Mdeep output file.}
}
\value{
A list containing \code{seqIDs} (a character vector of all unique
sequence IDs) and \code{tools} (a list of binary vectors for each tool).
}
\description{
Reads outputs from various tools and aggregates results into a structured
list. Noncoding predictions are marked as 1, coding as 0.
}
\examples{
# --- 1. Create temporary output files for a reproducible example ---
cpc2File <- tempfile()
plekFile <- tempfile()
write.table(data.frame(V1="ID1", V8="noncoding"), cpc2File, col.names=FALSE, row.names=FALSE)
write.table(data.frame(V1="ID2", V8="coding"), cpc2File, col.names=FALSE, row.names=FALSE, append=TRUE)
write.table(data.frame(V1="Non-coding", V3=">ID2"), plekFile, col.names=FALSE, row.names=FALSE)

# --- 2. Run the function with the temporary files ---
codpotResults <- codPotToTbl(cpc2Outfile = cpc2File, plekOutfile = plekFile)

print("Sequence IDs:")
print(codpotResults$seqIDs)
print("Tool predictions:")
print(codpotResults$tools)

# --- 3. Clean up the temporary files ---
unlink(c(cpc2File, plekFile))

}
\keyword{bioinformatics}
\keyword{coding}
\keyword{lncRNA}
\keyword{potential}
