% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/process_interactions.R
\name{process_interactions}
\alias{process_interactions}
\title{Process and Merge Genomic Interactions}
\usage{
process_interactions(
  gprof,
  interaction_table,
  type,
  lncRNA_col = NULL,
  target_col = NULL
)
}
\arguments{
\item{gprof}{data.frame. Genomic profiler data (e.g., output from gProfiler).
Must include at least the columns "term_name" and "intersection", and optionally "source".}

\item{interaction_table}{data.frame. Interaction data with columns for lncRNA and target identifiers.
Must include either "lncRNA.id" and "targetRNA.id", or columns specified by \code{lncRNA_col} and \code{target_col}.}

\item{type}{character. The type of interaction to assign (e.g., "cis" or "trans"). Must be specified.}

\item{lncRNA_col}{character, optional. Name of the column in \code{interaction_table} containing lncRNA identifiers.
Required if "lncRNA.id" is not present in \code{interaction_table}. Defaults to NULL.}

\item{target_col}{character, optional. Name of the column in \code{interaction_table} containing target identifiers.
Required if "targetRNA.id" is not present in \code{interaction_table}. Defaults to NULL.}
}
\value{
data.frame. A merged data frame containing the processed genomic profiler data and interaction data,
with unique rows. Includes columns such as "intersection", "term_name", "source", "type", and "lncRNA.id".
}
\description{
Processes genomic profiler data and merges it with an interaction table to create a unified
dataset of interactions. Requires specification of interaction type and ensures correct column
naming for lncRNA and target identifiers.
}
\examples{
# Example with default column names
gprof <- data.frame(term_name = "response to stress", intersection = "ENSMUSG00000000093", source = "GO:BP")
int_table <- data.frame(lncRNA.id = "ENSMUSG00000106858", targetRNA.id = "ENSMUSG00000000093")
result <- process_interactions(gprof, int_table, type = "trans")

# Example with custom column names
gprof2 <- data.frame(term_name = "cell activation", intersection = "ENSMUSG00000000731", source = "GO:BP")
int_table2 <- data.frame(Query = c("ENSMUSG00000002769", "ENSMUSG00000089736"), Target = "ENSMUSG00000000731")
result2 <- process_interactions(gprof2, int_table2, type = "trans", lncRNA_col = "Query", target_col = "Target")

}
