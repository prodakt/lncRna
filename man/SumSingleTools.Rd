% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/SumSingleTools.R
\name{sumSingleTools}
\alias{sumSingleTools}
\title{Summarize Coding Potential Results for Test Sets}
\usage{
sumSingleTools(codPotList, ncTest, cdsTest)
}
\arguments{
\item{codPotList}{A list object generated by \code{codPotToTbl()}. Must contain
\verb{$seqIDs} and \verb{$tools} (a named list of 0/1 prediction vectors).}

\item{ncTest}{A character vector of sequence IDs known to be non-coding.}

\item{cdsTest}{A character vector of sequence IDs known to be protein-coding.}
}
\value{
A list containing elements for sequences present in \code{ncTest} or
\code{cdsTest}:
\item{seqIDs}{Filtered character vector of sequence identifiers.}
\item{tools}{Filtered list of tool prediction vectors.}
\item{type}{Filtered character vector with annotation ("nc" or "cds").}
\item{isNC}{Filtered numeric vector: 1 for "nc", 0 for "cds".}
\item{sums}{Filtered numeric vector with the sum of predictions across all
tools.}
}
\description{
Filters and annotates coding potential results from \code{codPotToTbl} based on
provided non-coding and coding test sets. It calculates the sum of
non-coding predictions across tools for the filtered sequences.
}
\examples{
# --- 1. Create a mock object that mimics the output of codPotToTbl ---
mockCodPot <- list(
  seqIDs = c("ID1", "ID2", "ID3", "ID4", "ID5"),
  tools = list(
    CPC2 = c(1, 1, 0, 0, 1),
    PLEK = c(0, 1, 1, 0, 1)
  )
)

# --- 2. Define test sets ---
ncTestSet <- c("ID1", "ID2", "ID5")
cdsTestSet <- c("ID3")
# Note: ID4 is not in any test set and will be filtered out.

# --- 3. Run the function ---
summaryList <- sumSingleTools(
  codPotList = mockCodPot,
  ncTest = ncTestSet,
  cdsTest = cdsTestSet
)

print(summaryList)

}
\keyword{cds}
\keyword{filter}
\keyword{lncRNA}
\keyword{summary}
\keyword{validation}
