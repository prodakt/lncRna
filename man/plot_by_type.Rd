% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_by_type.R
\name{plot_by_type}
\alias{plot_by_type}
\title{Plot Sankey Diagram by Specified Interaction Types}
\usage{
plot_by_type(data, type, label = FALSE, color = NULL, title = NULL)
}
\arguments{
\item{data}{data.frame. A data frame containing gProfiler interaction results.
Must include columns: \code{term_name}, \code{intersection}, \code{lncRNA.id}, \code{type}, and optionally \code{source}.}

\item{type}{character vector, optional. Specifies the interaction types to include in the plot (e.g., "cis", "trans", "TAR").
Must match values in the \code{type} column of \code{data}. If missing, user will be prompted to choose from console.}

\item{label}{logical, optional. If \code{TRUE}, shows node labels. If \code{FALSE} (default), no labels are shown.}

\item{color}{character, optional. Single color for all nodes and links. If \code{NULL} (default), uses a random palette.}

\item{title}{character, optional. Diagram title. If \code{NULL} (default), no title is displayed.}
}
\value{
plotly object. A Sankey diagram visualizing filtered interactions.
}
\description{
Creates a Sankey diagram visualizing interactions from a gProfiler output table,
filtered by user-specified interaction types, with interactive type selection from the console if not specified.
Allows customization of labels, colors, and titles.
}
\examples{
# Assuming 'combined_table' is your gProfiler interaction data frame

# Example 1: Plot only "cis" interactions
# fig1 <- plot_by_type(data = combined_table, type = "cis")
# fig1 # Display the plot

# Example 2: Plot "cis" and "trans" interactions
# fig2 <- plot_by_type(data = combined_table, type = c("cis", "trans"), label = TRUE)
# fig2 # Display the plot

# Example 3: Plot custom interaction type with a title and color
# fig3 <- plot_by_type(data = combined_table, type = "TAR", title = "TAR Interactions", color = "blue")
# fig3 # Display the plot

# Example 4: Interactive type selection from console
# fig4 <- plot_by_type(data = combined_table) # User will be prompted to select type
# fig4 # Display the plot

}
