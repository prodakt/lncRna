% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotSankeyInteractions.R
\name{plotSankeyInteractions}
\alias{plotSankeyInteractions}
\title{Plot Functional Interactions as a Sankey Diagram}
\usage{
plotSankeyInteractions(
  interactionData,
  groupBy = NULL,
  selectIds = NULL,
  showLabels = FALSE,
  highlightSelected = FALSE,
  color = NULL,
  title = NULL
)
}
\arguments{
\item{interactionData}{A \code{data.frame} containing interaction data, typically
created by \code{annotateInteractions()}. Must contain columns: \code{"lncRNAId"},
\code{"intersection"}, \code{"term_name"}, and \code{"type"}.}

\item{groupBy}{A character string specifying the filtering criteria. Valid
options are: \code{"lncRNAId"}, \code{"target"}, \code{"term"}, \code{"type"}. If \code{NULL},
interactive selection is triggered.}

\item{selectIds}{Optional character vector of IDs/terms to filter or highlight.
If \code{NULL} and interactive, prompts for selection.}

\item{showLabels}{Logical. If \code{TRUE}, displays labels on nodes. Defaults to \code{FALSE}.}

\item{highlightSelected}{Logical. If \code{TRUE}, plots all data but colors only
the nodes/links associated with \code{selectIds}. If \code{FALSE} (default), plots
only the data matching \code{selectIds}.}

\item{color}{Optional character string. A single color for all nodes/links.
If \code{NULL}, a random palette (Polychrome) is generated.}

\item{title}{Optional character string for the plot title.}
}
\value{
A \code{plotly} object containing the Sankey diagram.
}
\description{
Visualizes genomic interaction data (lncRNAId -> Target -> Functional Term)
as an interactive Sankey diagram using Plotly. Replaces \code{plot_by_terms},
\code{plot_by_lnc}, etc., providing a unified interface for filtering and plotting.
}
\examples{
# --- 1. Create mock interaction data ---
mockData <- data.frame(
  lncRNAId = c(rep("LNC1", 3), rep("LNC2", 2)),
  intersection = c("T1", "T2", "T1", "T3", "T2"),
  term_name = c("Stress", "Growth", "Stress", "Immunity", "Growth"),
  type = c("cis", "cis", "trans", "trans", "cis"),
  stringsAsFactors = FALSE
)

# --- 2. Run in non-interactive mode ---

# Example 1: Filter by specific Term
fig1 <- plotSankeyInteractions(
  interactionData = mockData,
  groupBy = "term",
  selectIds = "Stress",
  title = "Stress Interactions"
)
# fig1

# Example 2: Highlight specific lncRNAId
fig2 <- plotSankeyInteractions(
  interactionData = mockData,
  groupBy = "lncRNAId",
  selectIds = "LNC1",
  highlightSelected = TRUE,
  title = "Highlighting LNC1"
)
# fig2

}
