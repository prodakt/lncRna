% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotClockMetrics.R
\name{plotClockMetrics}
\alias{plotClockMetrics}
\title{Create Clock Plots for Confusion Matrix Metrics}
\usage{
plotClockMetrics(
  cmList,
  methods = NULL,
  metrics = c("Accuracy", "Sensitivity", "Specificity", "Precision", "Recall"),
  plotTitle = "Clock Plot of Metrics",
  colors = NULL,
  layout = "single",
  ...
)
}
\arguments{
\item{cmList}{A named list where each element represents a confusion matrix,
typically the output from \code{calculateCM}.}

\item{methods}{An optional character vector of method names to include.
If \code{NULL}, interactive selection is triggered in an interactive session;
otherwise, all methods are used.}

\item{metrics}{A character vector of metrics to display. Defaults to
\code{c("Accuracy", "Sensitivity", "Specificity", "Precision", "Recall")}.}

\item{plotTitle}{The title for the plot(s).}

\item{colors}{An optional vector of colors for each method.}

\item{layout}{The layout of the plots: \code{"single"} (default) for one plot,
or \code{"multiple"} for a grid of plots.}

\item{...}{Additional arguments (not currently used).}
}
\value{
A \code{ggplot} object (for \code{layout = "single"}) or a \code{patchwork}
object (for \code{layout = "multiple"}), which can be printed to display.
}
\description{
Generates circular bar plots (clock plots) to visualize performance metrics.
Supports both single and multiple plot layouts for comparing methods.
}
\examples{
# --- 1. Create a mock cmList object (as from calculateCM) ---
set.seed(123)
mockCmList <- list(
  `MethodA` = list(metrics = c(Accuracy=0.9, Sensitivity=0.8, Specificity=0.95,
                               Precision=0.85, Recall=0.8)),
  `MethodB` = list(metrics = c(Accuracy=0.8, Sensitivity=0.9, Specificity=0.7,
                               Precision=0.75, Recall=0.9))
)

# --- 2. Run the plot function ---
# Example 1: Single plot comparing selected methods
plotClockMetrics(
  cmList = mockCmList,
  methods = c("MethodA", "MethodB"),
  plotTitle = "Comparison Clock Plot"
)

# Example 2: Multiple plots, one for each method
plotClockMetrics(cmList = mockCmList, layout = "multiple")

}
