% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aggregateCodPot.R
\name{aggregateCodPot}
\alias{aggregateCodPot}
\title{Aggregate Coding Potential Tool Results}
\usage{
aggregateCodPot(
  CPC2_outfile = NULL,
  PLEK_outfile = NULL,
  FEELnc_outfile = NULL,
  CPAT_outfile = NULL,
  CPAT_cutoff = 0.364,
  CNCI_outfile = NULL,
  LncFinder_outfile = NULL,
  lncRNA_Mdeep_outfile = NULL
)
}
\arguments{
\item{CPC2_outfile}{Path to the CPC2 output file.}

\item{PLEK_outfile}{Path to the PLEK output file.}

\item{FEELnc_outfile}{Path to the FEELnc output file.}

\item{CPAT_outfile}{Path to the CPAT output file.}

\item{CPAT_cutoff}{Cutoff value for CPAT probability (default: 0.364).}

\item{CNCI_outfile}{Path to the CNCI output file.}

\item{LncFinder_outfile}{Path to the LncFinder output file.}

\item{lncRNA_Mdeep_outfile}{Path to the lncRNA_Mdeep output file.}
}
\value{
A list containing \code{seqIDs} (a character vector of all unique
sequence IDs found across all provided files) and \code{tools} (a list where
each element is a binary vector of predictions for a given tool).
}
\description{
Reads output files from various coding potential prediction tools and
aggregates their results into a structured list. Non-coding predictions are
encoded as 1, and coding predictions as 0.
}
\examples{
# --- 1. Create temporary output files for a reproducible example ---
cpc2File <- tempfile()
plekFile <- tempfile()

# CPC2 format: ID ... classification
write.table(
  data.frame(V1="ID1", V8="noncoding"),
  cpc2File, col.names=FALSE, row.names=FALSE, sep="\t"
)
write.table(
  data.frame(V1="ID2", V8="coding"),
  cpc2File, col.names=FALSE, row.names=FALSE, sep="\t", append=TRUE
)

# PLEK format: Classification Score >ID
write.table(
  data.frame(V1="Non-coding", V2="0.9", V3=">ID2"),
  plekFile, col.names=FALSE, row.names=FALSE, sep=" "
)

# --- 2. Run the function with the temporary files ---
codpotResults <- aggregateCodPot(CPC2_outfile = cpc2File, PLEK_outfile = plekFile)

print("Sequence IDs:")
print(codpotResults$seqIDs)
print("Tool predictions:")
print(codpotResults$tools)

# --- 3. Clean up the temporary files ---
unlink(c(cpc2File, plekFile))
}
