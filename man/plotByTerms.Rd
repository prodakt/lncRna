% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sankeyPlots.R
\name{plotByTerms}
\alias{plotByTerms}
\alias{plotByTarget}
\alias{plotByLnc}
\alias{plotByType}
\title{Plot Sankey Diagram for gProfiler Term Connections}
\usage{
plotByTerms(
  data,
  selectedTerms = NULL,
  showLabel = FALSE,
  nodeColor = NULL,
  title = NULL,
  colorSelected = FALSE
)

plotByTarget(
  data,
  selectedTarget = NULL,
  showLabel = FALSE,
  nodeColor = NULL,
  title = NULL,
  colorSelected = FALSE
)

plotByLnc(
  data,
  selectedLnc = NULL,
  showLabel = FALSE,
  nodeColor = NULL,
  title = NULL,
  colorSelected = FALSE
)

plotByType(
  data,
  interactionTypes = NULL,
  showLabel = FALSE,
  nodeColor = NULL,
  title = NULL
)
}
\arguments{
\item{data}{A data.frame from interaction processing functions. Must include
\code{term_name}, \code{intersection}, \code{lncRNAId}, and \code{type} columns.}

\item{selectedTerms}{A character vector of term names to highlight or plot.
If \code{NULL} (default) and the session is interactive, the user is prompted
to select terms. In a non-interactive session, \code{NULL} results in all
terms being plotted.}

\item{showLabel}{A logical value; if \code{TRUE}, node labels are displayed.}

\item{nodeColor}{A single color for all nodes. If \code{NULL} (default), a
random palette is used.}

\item{title}{A character string for the diagram title.}

\item{colorSelected}{A logical value. If \code{TRUE}, all interactions are plotted,
but only those related to \code{selectedTerms} are colored. If \code{FALSE}
(default), only interactions involving \code{selectedTerms} are plotted.}

\item{selectedTarget}{A character vector of target gene IDs to highlight or plot.}

\item{selectedLnc}{A character vector of lncRNA IDs to highlight or plot.}

\item{interactionTypes}{A character vector specifying the interaction types to plot.}
}
\value{
A \code{plotly} object representing the Sankey diagram, or
\code{invisible(NULL)} if required packages are not installed.
}
\description{
Creates a Sankey diagram visualizing connections to selected gProfiler terms.
Requires the 'plotly' and 'Polychrome' packages.
}
\examples{
# --- 1. Create a sample data frame for all examples ---
sampleData <- data.frame(
  term_name = c("GO:1", "GO:2", "GO:3", "GO:2"),
  intersection = c("GeneA", "GeneB", "GeneC", "GeneA"),
  lncRNAId = c("Lnc1", "Lnc1", "Lnc2", "Lnc2"),
  type = c("cis", "trans", "cis", "trans"),
  stringsAsFactors = FALSE
)

# --- 2. Demonstrate plotting functions (will run only if packages are available) ---
if (requireNamespace("plotly", quietly = TRUE) &&
    requireNamespace("Polychrome", quietly = TRUE)) {

  fig_terms <- plotByTerms(data = sampleData, selectedTerms = "GO:2")
  # To display the plot in an interactive session, simply type `fig_terms`

  fig_target <- plotByTarget(data = sampleData, selectedTarget = "GeneA")

} else {
  message("Skipping Sankey plot examples because 'plotly' or 'Polychrome' is not installed.")
}
}
