% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_by_target.R, R/sankey_plots.R
\name{plot_by_target}
\alias{plot_by_target}
\title{Plot Sankey Diagram for Selected Target Genes}
\usage{
plot_by_target(
  data,
  select_target = NULL,
  label = FALSE,
  color = NULL,
  title = NULL,
  color_selected = FALSE
)

plot_by_target(
  data,
  select_target = NULL,
  label = FALSE,
  color = NULL,
  title = NULL,
  color_selected = FALSE
)
}
\arguments{
\item{data}{data.frame. A data frame containing combined output from trans/cis_interactions functions.
Must include columns: \code{term_name}, \code{intersection}, \code{lncRNA.id}, \code{type}, and optionally \code{source}.}

\item{select_target}{character vector, optional. Target gene IDs from \code{intersection} to highlight or plot. If \code{NULL} (default), prompts interactive selection.}

\item{label}{logical, optional. If \code{TRUE}, shows node labels. If \code{FALSE} (default), no labels.}

\item{color}{character, optional. Single color for all nodes/links. If \code{NULL} (default), uses a random palette.}

\item{title}{character, optional. Diagram title. If \code{NULL} (default), no title.}

\item{color_selected}{logical, optional. With \code{select_target}, if \code{TRUE}, plots all data but colors only selected targets. If \code{FALSE} (default), plots only selected targets.}
}
\value{
plotly object. A Sankey diagram visualizing target gene interactions.

plotly object. Sankey diagram of target connections.
}
\description{
Creates a Sankey diagram visualizing interactions connected to specific target protein-coding genes
from a gProfiler output table. Allows highlighting or exclusive plotting of selected targets with
customizable labeling, coloring, and titles.

Creates a Sankey diagram visualizing the connections of nodes to selected target protein-coding genes from a gProfiler output table.
}
\examples{
# Assuming 'combined_table' is your gProfiler interaction data frame
# fig <- plot_by_target(data = combined_table, select_target = c("gene1", "gene2"), color_selected = TRUE)
# fig # Display the plot

}
