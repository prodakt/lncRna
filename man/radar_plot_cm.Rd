% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/radar_plot_cm.R
\name{radar_plot_cm}
\alias{radar_plot_cm}
\title{Create Radar Plots for Confusion Matrix Metrics}
\usage{
radar_plot_cm(
  cm_list,
  methods = NULL,
  metrics = c("Accuracy", "Sensitivity", "Specificity", "Precision", "Recall"),
  plot_title = "Radar Plot of Metrics",
  colors = NULL,
  layout = "single",
  display_area = FALSE,
  display_fill = T,
  save_data = F,
  file_name = NULL,
  ...
)
}
\arguments{
\item{cm_list}{list. A named list of confusion matrix objects, typically output from
\code{caret::confusionMatrix}. List names should correspond to the method names.}

\item{methods}{character vector, optional. Vector of method names to be included in the plot.
If NULL, the function will provide an interactive prompt to select methods from \code{cm_list}.}

\item{metrics}{character vector. Metrics to be visualized on the radar plot axes.
Defaults to c("Accuracy", "Sensitivity", "Specificity", "Precision", "Recall").
Valid options include metrics from \code{confusionMatrix} output like "Accuracy", "Sensitivity",
"Specificity", "Pos Pred Value", "Neg Pred Value", "Precision", "Recall", "F1",
"Prevalence", "Detection Rate", "Detection Prevalence", "Balanced Accuracy",
and any other metric available in \code{confusion_matrix_obj$overall} or \verb{$byClass}.}

\item{plot_title}{character, optional. Title of the radar plot. Defaults to "Radar Plot of Metrics".}

\item{colors}{character vector, optional. Colors to be used for each method's radar line and fill.
If NULL, default colors from \code{scales::hue_pal()} are used. If fewer colors than methods are provided,
colors are recycled.}

\item{layout}{character, optional. Specifies the plot layout:
"single": Creates a single radar plot comparing all selected methods.
"multiple": Generates a series of radar plots, one for each method.
Defaults to "single".}

\item{display_area}{logical, optional. If TRUE, displays the polygon area for each method,
either in the legend for single plots or below each plot in multiple layouts. Defaults to FALSE.}

\item{display_fill}{logical, optional. If TRUE, fills the radar chart polygons with a semi-transparent color
corresponding to each method. If FALSE, polygons are not filled. Defaults to TRUE.}

\item{save_data}{logical, optional. If TRUE, saves the radar data frame to a CSV file. Defaults to FALSE.}

\item{file_name}{character, optional. The name of the file to save the radar data frame. Required if \code{save_data} is TRUE.}

\item{...}{Additional arguments passed to the \code{fmsb::radarchart} function for customization
of the radar plot appearance.}
}
\value{
ggplot object or NULL.
Returns \code{NULL} as the function primarily produces plots as side effects.
When \code{layout = "single"}, the plot is directly printed.
When \code{layout = "multiple"}, multiple plots are generated and arranged using \code{par(mfrow=...)}.
}
\description{
Generates radar plots (also known as spider or star plots) to visualize and compare
performance metrics from confusion matrices of different methods. Supports single plot
to compare multiple methods or multiple plots to evaluate each method individually.
}
\examples{
# Assuming 'all_cms' is a named list of confusionMatrix objects

# Example 1: Single radar plot comparing methods, area in legend, no fill
radar_plot_cm(all_cms, plot_title = "Comparison Radar Plot", display_area = TRUE, display_fill = FALSE)

# Example 2: Multiple radar plots, one per method, with area and fill
radar_plot_cm(all_cms, layout = "multiple", plot_title = "Radar Plots per Method", display_area = TRUE, display_fill = TRUE)

# Example 3: Single plot, specific methods and metrics, custom colors
radar_plot_cm(all_cms, methods = c("Method1", "Method3"),
               metrics = c("Accuracy", "F1", "Specificity"),
               plot_title = "Custom Metric Radar Plot",
               colors = c("skyblue", "salmon"))

# Example 4: Multiple plots, different metrics, no area display
radar_plot_cm(all_cms, layout = "multiple",
               metrics = c("Precision", "Recall", "Balanced Accuracy"),
               plot_title = "Multiple Radar Plots - Alt Metrics",
               display_area = FALSE)

# Example 5: Single plot, interactive method selection will be prompted if methods=NULL
# radar_plot_cm(all_cms, plot_title = "Interactive Method Radar Plot") # Uncomment to run interactively

}
