% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_lion_analysis.R
\name{run_lion_analysis}
\alias{run_lion_analysis}
\title{Run LION RNA-Protein Interaction Analysis}
\usage{
run_lion_analysis(
  rna_seqs,
  prot_seqs,
  output_filename = "LION_results.csv",
  parallel_cores = 8
)
}
\arguments{
\item{rna_seqs}{Named list of RNA sequences, typically from \code{seqinr::read.fasta}.}

\item{prot_seqs}{Named list of protein sequences, typically from \code{seqinr::read.fasta}.}

\item{output_filename}{Character string specifying the output CSV file name (default: "LION_results.csv").}

\item{parallel_cores}{Integer specifying the number of CPU cores for parallel processing (default: 8).}
}
\value{
A data frame containing the cumulative LION prediction results.
}
\description{
Performs RNA-protein interaction predictions using the LION package by processing all
combinations of RNA and protein sequences in pairs. Results are saved iteratively to a CSV file.
}
\examples{
\dontrun{
  # Load example sequence files
  rna_seqs <- seqinr::read.fasta("rna_sequences.fa", seqtype = "RNA", as.string = TRUE)
  prot_seqs <- seqinr::read.fasta("protein_sequences.fa", seqtype = "AA", as.string = TRUE)
  
  # Run LION analysis
  results <- run_lion_analysis(rna_seqs, prot_seqs, output_filename = "my_results.csv", parallel_cores = 4)
}
}
