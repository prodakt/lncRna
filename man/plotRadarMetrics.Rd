% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotRadarMetrics.R
\name{plotRadarMetrics}
\alias{plotRadarMetrics}
\title{Create Radar Plots for Confusion Matrix Metrics}
\usage{
plotRadarMetrics(
  cmList,
  methods = NULL,
  metrics = c("Accuracy", "Sensitivity", "Specificity", "Precision", "Recall"),
  plotTitle = "Radar Plot of Metrics",
  colors = NULL,
  layout = "single",
  displayArea = FALSE,
  displayFill = TRUE,
  saveData = FALSE,
  fileName = NULL,
  ...
)
}
\arguments{
\item{cmList}{A named list where each element represents a confusion matrix,
typically the output from \code{calculateCM}. Each element must be a list
containing at least a named numeric vector called \verb{$metrics}.}

\item{methods}{An optional character vector of method names (from \code{names(cmList)})
to include in the plot. If \code{NULL}, behavior depends on the session:
interactive mode prompts for selection, while non-interactive mode uses
all available methods.}

\item{metrics}{A character vector of metrics to visualize on the radar plot axes.
Defaults to \code{c("Accuracy", "Sensitivity", "Specificity", "Precision", "Recall")}.}

\item{plotTitle}{A character string for the plot title.}

\item{colors}{An optional character vector of colors for each method. If \code{NULL},
default colors are generated.}

\item{layout}{Specifies the plot layout: \code{"single"} (default) for one plot
comparing all methods, or \code{"multiple"} for a grid of individual plots.}

\item{displayArea}{Logical. If \code{TRUE}, displays the calculated polygon area.}

\item{displayFill}{Logical. If \code{TRUE} (default), fills radar chart polygons
with a semi-transparent color.}

\item{saveData}{Logical. If \code{TRUE}, saves the underlying data frame to a CSV file.}

\item{fileName}{The name of the file for saving data (required if \code{saveData} is \code{TRUE}).}

\item{...}{Additional arguments passed to \code{fmsb::radarchart}.}
}
\value{
This function is called for its side effect of generating plots and
does not return a value (\code{invisible(NULL)}).
}
\description{
Generates radar plots to visualize and compare performance metrics for
different methods or tool combinations. Supports a single plot for comparing
multiple methods or a grid of plots for individual evaluation.
}
\examples{
# --- 1. Create a mock cmList object (as from calculateCM) ---
set.seed(123)
mockCmList <- list(
  `MethodA` = list(metrics = c(Accuracy=0.9, Sensitivity=0.8, Specificity=0.95,
                               Precision=0.85, Recall=0.8)),
  `MethodB` = list(metrics = c(Accuracy=0.8, Sensitivity=0.9, Specificity=0.7,
                               Precision=0.75, Recall=0.9)),
  `MethodC` = list(metrics = c(Accuracy=0.85, Sensitivity=0.85, Specificity=0.85,
                               Precision=0.85, Recall=0.85))
)

# --- 2. Run the plot function ---
# To prevent plots from showing up during automated checks, we wrap in a device
temp_png <- tempfile(fileext = ".png")
png(temp_png)

# Example 1: Single plot comparing selected methods
plotRadarMetrics(
  cmList = mockCmList,
  methods = c("MethodA", "MethodC"),
  plotTitle = "Comparison Plot"
)

# Example 2: Multiple plots, one for each method
plotRadarMetrics(cmList = mockCmList, layout = "multiple")

dev.off()
unlink(temp_png)

}
