% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/testTrainCds.R
\name{splitCdsTestTrain}
\alias{splitCdsTestTrain}
\title{Split Protein-Coding Sequences into Test and Train Sets}
\usage{
splitCdsTestTrain(cdsIds, trainProportion = 0.6)
}
\arguments{
\item{cdsIds}{A character vector of protein-coding sequence identifiers, or
an object (like a list from \code{seqinr::read.fasta}) from which names can be
extracted.}

\item{trainProportion}{The proportion (0 to 1) of sequences to allocate to the
training set (default: 0.6).}
}
\value{
A list containing two character vectors:
\item{train}{Sequence identifiers allocated to the training set.}
\item{test}{Sequence identifiers allocated to the testing set.}
}
\description{
Splits a set of protein-coding sequence identifiers into training and
testing subsets based on a specified proportion. For reproducible splits,
set the random seed using \code{set.seed()} before calling this function.
}
\examples{
# --- 1. Define a character vector of sequence IDs ---
cdsIdsVec <- paste0("cds_seq_", 1:200)

# --- 2. Run the function with a 50/50 split ---
set.seed(456) # for a reproducible example
cdsSplit <- splitCdsTestTrain(cdsIds = cdsIdsVec, trainProportion = 0.5)

# --- 3. Verify the output ---
print(paste("Training set size:", length(cdsSplit$train)))
print(paste("Test set size:", length(cdsSplit$test)))
head(cdsSplit$train)

}
\keyword{data}
\keyword{learning}
\keyword{machine}
\keyword{partitioning}
\keyword{sampling}
\keyword{split}
