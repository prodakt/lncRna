% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotFourfoldCm.R
\name{plotFourfoldCm}
\alias{plotFourfoldCm}
\title{Create Fourfold Plots for Confusion Matrices}
\usage{
plotFourfoldCm(
  cmList,
  methods = NULL,
  plotTitle = "Fourfold Plot of Confusion Matrix",
  colors = c("#cc6666", "#99cc99"),
  conf.level = 0,
  std = "margins",
  ...
)
}
\arguments{
\item{cmList}{A named list where each element represents a confusion matrix,
typically the output from \code{calculateCM}. Must contain \verb{$table}.}

\item{methods}{An optional character vector of method names to include.
If \code{NULL}, interactive selection is triggered in an interactive session.}

\item{plotTitle}{The base title for the plot(s).}

\item{colors}{A vector of two colors. The first color is for the false
diagonal (errors: FP, FN), and the second is for the true diagonal
(correct predictions: TP, TN). Defaults to pastel red and green.}

\item{conf.level}{Confidence level for the rings. Set to \code{0} (default)
to hide the rings for a cleaner presentation on slides/reports.}

\item{std}{Character string specifying how to standardize the table.
Defaults to \code{"margins"}.}

\item{...}{Additional arguments passed to \code{graphics::fourfoldplot}.}
}
\value{
This function is called for its side effect of generating plots and
does not return a value (\code{invisible(NULL)}).
}
\description{
Generates fourfold plots to visualize the confusion matrix (TP, TN, FP, FN)
for selected methods. Provides a clear, intuitive visual representation of
prediction accuracy and error distribution.
}
\examples{
# --- Assuming 'allCms' is generated by calculateCM ---
# Example 1: Interactive selection (if run in console)
# plotFourfoldCm(cmList = allCms)

# Example 2: Plot specific combinations on a grid
# plotFourfoldCm(cmList = allCms, methods = c("CPC2+PLEK", "PLEK+CPAT+CNCI"))

}
